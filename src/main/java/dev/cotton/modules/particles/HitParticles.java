package dev.cotton.modules.particles;
import dev.cotton.core.Module;
public class HitParticles extends Module {
    public HitParticles() { super("Hit Particles", "Particles burst on every hit", Category.PARTICLES); }
}
